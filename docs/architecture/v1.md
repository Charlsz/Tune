# Tune — Arquitectura v1

**Versión:** 1.1  
**Fecha:** 2026-08-29  
**Basado en:** [Primer Informe](../PrimerInforme.md) — sección 5

## Visión general

Tune implementa un laboratorio MLOps modular para **fine-tuning eficiente**: ejecuta un baseline y una estrategia optimizada sobre el mismo dataset y el mismo modelo preentrenado, registra cada corrida (parámetros, tiempo, memoria, calidad), compara ambas y sirve el modelo seleccionado mediante **API REST** o CLI.

El modelo concreto (incluido un caso geoespacial, si es viable) valida la arquitectura; no es el eje del sistema.

## Diagrama de arquitectura

![Arquitectura](../assets/img/Arquitectura.jpg)

## Flujo de datos

```text
Dataset versionado + modelo preentrenado
   ↓
Configuración (baseline | optimized)
   ↓
Fine-tuning
   ↓
Tracking (parámetros, métricas, tiempo, memoria, GPU-hours)
   ↓
Evaluation (mismo conjunto de prueba)
   ↓
Comparación de las dos corridas
   ↓
Registry / promoción
   ↓
API o CLI de inferencia
   ↓
Demo
```

## Componentes

| Componente | Tecnología | Ubicación en repo | Estado |
|------------|------------|-------------------|--------|
| Dataset management | Scripts + metadatos de versión | `data/`, `src/tune/infrastructure/data/` | Esqueleto |
| Training engine | PyTorch Lightning / PEFT (intercambiable) | `configs/training/`, `src/tune/infrastructure/training/` | Esqueleto (Fase 1) |
| Estrategias | YAML baseline y optimized | `configs/training/` | Borrador (caso preferido) |
| Experiment tracking | MLflow | `src/tune/infrastructure/tracking/` | Esqueleto |
| Model registry | MLflow Model Registry | `src/tune/infrastructure/registry/` | Esqueleto |
| Comparison | Tabla/gráfico baseline vs optimized | `src/tune/application/stages/compare.py` | Esqueleto |
| Evaluation | Métricas de la tarea del caso | `src/tune/infrastructure/evaluation/` | Esqueleto (Fase 1) |
| Pipeline | CLI `tune` + script | `src/tune/interfaces/cli/`, `scripts/run_pipeline.py` | Esqueleto |
| Inference | Predictor desde registry | `src/tune/infrastructure/inference/` | Esqueleto (Fase 5) |
| API | FastAPI (`/health`, `/model`, `/predict`) | `src/tune/interfaces/api/` | Esqueleto |
| Domain / policies | Umbrales y promoción | `src/tune/domain/` | Implementado |
| Containers | Docker Compose | `docker/`, `docker-compose.yml` | Esqueleto |
| Demo | Cliente mínimo o CLI | `demo/` | Opcional |

## Caso de estudio

El caso no está fijado como dependencia. El preferido inicial puede reutilizar trabajo geoespacial explorado antes (dataset + modelo preentrenado) si el cómputo lo permite. El respaldo es un modelo y un dataset más livianos.

Ver [ADR 001](../decisions/001-task-selection.md).

## Separación de responsabilidades

Siguiendo el primer informe (sección 5):

- **Infraestructura MLOps:** pipeline, tracking, registry, comparación, API. No “hace” el entrenamiento más rápido.
- **Estrategias evaluables:** baseline vs optimizado (LoRA, mixed precision, etc.). Explican el ahorro, si existe.
- **Caso de estudio:** dataset, modelo preentrenado, métricas de calidad e input/output de `/predict`.

Cambiar el caso no debe exigir rediseñar el núcleo. No se atribuye a Tune un speedup que produzca una técnica concreta.

## Entorno de ejecución

| Fase | Entorno |
|------|---------|
| Desarrollo / smoke tests | Colab, Kaggle o máquina local con GPU |
| Entrenamiento baseline y optimized | El mismo hardware de referencia, documentado |
| Pipeline MLOps + MLflow | Máquina local o servidor con persistencia |
| API + Docker | Local / contenedor |

## Decisiones de arquitectura

- [ADR 001 — Caso de estudio](../decisions/001-task-selection.md)
- [ADR 002 — Orquestación](../decisions/002-orchestration.md)

## Evolución

| Versión | Cambios |
|---------|---------|
| v1.0 | Borrador Terra (ciclo de vida Prithvi). Superado. |
| v1.1 | Tune: laboratorio de fine-tuning eficiente, caso de estudio desacoplado |
| v1.2 | Integración MLflow, comparación baseline vs optimized, pipeline |
| v1.3 | API + contenedores |
| v1.4 | Segundo caso de estudio (opcional, solo si el mínimo ya está cerrado) |
