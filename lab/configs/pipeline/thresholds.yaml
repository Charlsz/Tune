# Criterios de promoción / rechazo (protocolo experimental, ADR 003).
# Valores de trabajo de la investigación; cambiar solo con evidencia y PR al ADR.

# Métrica primaria del Evaluator (segmentación: mIoU; Plan B: accuracy):
min_primary_metric: 0.60

# Caída máxima absoluta de optimized vs baseline (null = no aplica):
max_quality_drop_vs_baseline: 0.02
