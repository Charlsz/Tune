# Plantilla de metadatos para datasets versionados fuera de Git.
# Copiar a data/<name>/<version>/metadata.yaml o generar con:
#   python lab/scripts/prepare_data.py --name <name> --version <ver> --init-layout

name: hls_burn_scars
version: "1.0"
source: https://huggingface.co/datasets/ibm-nasa-geospatial/hls_burn_scars
downloaded_at: null
license: dataset-dependent
splits: [train, val, test]
sample_counts:
  train: 0
  val: 0
  test: 0
checksum: null
notes: "Rellenar sample_counts y checksum tras poblar los splits."
