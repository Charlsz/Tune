"""Evaluadores (``Evaluator``) por tipo de tarea.

Un módulo por tarea del caso de estudio (p. ej. ``segmentation.py`` con IoU/mIoU/F1).
El stage ``evaluate`` no sabe qué tarea es; solo recibe ``QualityMetrics``.
"""
